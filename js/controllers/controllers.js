var appControllers = angular.module('appControllers', []);

appControllers.controller('homepage-ctrl', ['$scope',
    function ($scope) {
        //Do nothing right now
    }]);

appControllers.controller('prodsnsrvcs-ctrl', ['$scope',
    function ($scope) {
        //Do nothing right now
    }]);
